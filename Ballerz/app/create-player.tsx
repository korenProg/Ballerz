import { View, Text } from 'react-native';

export default function CreatePlayerScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Create Player Screen</Text>
    </View>
  );
}
